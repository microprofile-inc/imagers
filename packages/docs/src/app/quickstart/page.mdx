import { PackageName } from '@/components/PackageName'

export const metadata = {
  title: 'Quickstart',
  description:
    '安装 @microprofile/imagers',
}

# Quickstart

安装 `@microprofile/imagers`

## 安装

<CodeGroup>
```bash {{ title: 'pnpm' }}
pnpm add @microprofile/imagers
```

```bash {{ title: 'npm' }}
npm install @microprofile/imagers
```
</CodeGroup>

## 初始化

<div className={'flex'}>
    使用 <PackageName /> 处理图片前，请先初始化wasm
</div>

```typescript {{ title: '初始化' }}
import {init} from '@microprofile/imagers';

await init();
// some code
```

```typescript {{ title: '或者' }}
import {init} from '@microprofile/imagers';

function Some() {
    init().then(() => {
        // some code
    })
}
```
